{% extends "layout.njk" %}

{% block content %}

  <div class="container">
    <div class="info">
         <img src="/Bruno.jpg" alt="Minha foto" width=300 height=430>
      <div class="text-info">
      <h1>{{ about.name }}</h1>
      <h3>{{ about.role }}</h3>

      <p> {{ about.description }} </p>
       
      </div>
    </div>
    <footer>
      <ul>
       {% for link in about.link %}
        <li><a href="{{link.url}}" target="_blank">{{link.name}}</a></li>
       {% endfor %}
    </ul>
   </footer> 

   </div>

{% endblock %}